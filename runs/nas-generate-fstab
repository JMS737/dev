#!/usr/bin/env bash

if [ ! -d /nas ]; then
	mkdir /nas
fi

if [ -z $1 ]; then
	echo "Usage 'nas-fstab <hostname>'"
	exit
fi

HOST=$1
CRED_DIR=$XDG_DATA_HOME/nas
CRED_FILE=$CRED_DIR/smb-credentials

echo "
//$HOST/vault /nas/vault cifs defaults,credentials=$CRED_FILE,uid=$UID
//$HOST/scratch /nas/scratch cifs defaults,credentials=$CRED_FILE,uid=$UID
//$HOST/media /nas/media cifs defaults,credentials=$CRED_FILE,uid=$UID
//$HOST/shared /nas/shared cifs defaults,credentials=$CRED_FILE,uid=$UID
//$HOST/games /nas/games cifs defaults,credentials=$CRED_FILE,uid=$UID
//$HOST/archives /nas/archives cifs defaults,credentials=$CRED_FILE,uid=$UID
"
